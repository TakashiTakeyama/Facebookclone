<p id="notice"><%= notice %></p>

<h1>投稿記事一覧</h1>

<table>
  <thead>
    <tr>
      <th>投稿記事</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pictures.each do |picture| %>
      <tr>
        <td><%= image_tag picture.image.url, :size =>'300x300' %></td>
        <td><%= picture.content %></td>
        <td><%= link_to 'Show',  picture, class: "btn btn-primary" %></td>
        <td><%= link_to 'Edit', edit_picture_path(picture),class: "btn btn-warning" %></td>
        <td><%= link_to 'Destroy', picture, class: "btn btn-warning", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新しい記事を投稿する', new_picture_path %>